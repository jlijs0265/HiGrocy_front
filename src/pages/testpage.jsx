import SideBar from "../components/SideBar";

const TestPage = () => {
  return (
    <div>
      {/* 아래에 테스트할 컴포넌트 입력후 저장,
       /test로 접속하여 확인 Test 후 git commit시에는
        discard change(변경사항 없음)으로 돌려놓고 commit할것  */}
    </div>
  );
};

export default TestPage;
